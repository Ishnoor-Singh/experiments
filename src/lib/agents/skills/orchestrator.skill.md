# Orchestrator Agent SOP

## Step 1: Delegate to principal-developer
Call `delegate_to_agent` with:
- agent: "principal-developer"
- task: Pass through the user's app description
- context: Include any relevant details

That's it. One tool call, then stop.
